resolver: lts-10.10

packages:
  - serverless-execute
  - serverless-execute-aws-lambda

nix:
  packages:
    - zlib
    - zlibStatic.static
    - glibc
    - glibc.static
    - (gmp.override { withStatic = true; })
    - unzip
